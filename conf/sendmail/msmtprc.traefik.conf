account default
auth login
tls on
tls_starttls on
tls_trust_file /etc/ssl/certs/ca-certificates.crt
host ${SMTP_HOST}
port ${SMTP_PORT}
from ${SMTP_EMAIL}
user ${SMTP_EMAIL}
password ${SMTP_PASSWORD}
